---
shellcheck:
  stage: shellcheck
  image: koalaman/shellcheck-alpine:stable
  script:
    - find . -type f -name "*.sh" -print0 | xargs -0 shellcheck
  rules:
    - changes:
        - "**/*.sh"
  allow_failure: false
