---
- name: "Clean p8 app runtime logs"
  hosts: all
  remote_user: root
  gather_facts: False

  # 根据停服顺序删除
  # login -> gate -> game -> cross -> gm -> global -> log -> zk -> api
  roles:
    - login
    - gate
    - game
    - cross
